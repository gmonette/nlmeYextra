#' Extra pdClasses and varClasses for special models using nlme
#' 
#' nlmeYextra provides an new pdClass, `pdInd`, that models random effects that are independent of each other but not necessarily independent of the random intercepts.  The package also provides a variance class  variables whose 
#'
nlmeYextra <- function() NULL